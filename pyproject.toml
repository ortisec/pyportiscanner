[project]
name = "pyportscanner"
version = "0.1.0"
description = "TCP Port Scanner básico en Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
pps = "pyportscanner.cli:main"